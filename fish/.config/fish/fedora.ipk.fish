echo "Now you're fishing with style"
set -gx TERMINAL gnome-terminal

#set PATH $PATH "~/.local/arduino-1.8.5/"
#set PATH $PATH "~/.local/LSP/cquery/build/release/bin/"

source (conda info --root)/etc/fish/conf.d/conda.fish
