[Unit]
Description=Lock the screen on resume from suspend
Before=sleep.target suspend.target

[Service]
User=hgf
Type=forking
Environment=DISPLAY=:0
ExecStart=/usr/bin/i3lock -c 404040

[Install]
WantedBy=sleep.target suspend.target
