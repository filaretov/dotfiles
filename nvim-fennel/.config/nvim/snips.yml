_global:
  todo: 'TODO (hgf): '
  test: "testestest"

c:
  hin: '#include "$0"'
  sin: '#include <$0>'
  hg: |-
    #ifndef ${-1=vim.fn.expand('%:t:r'):upper() .. '_H'}
    #define ${-1}
    $0
    #endif // ${-1}
  u8: 'uint8_t'
  u32: 'uint32_t'
  main: |-
    int main(void) {
        puts("Hello, World!");
        return EXIT_SUCCESS;
    }
  pf: 'printf("$0\n");'
  for: |-
    for (int i = 0; i < $1; i++) {
        $0
    }

python:
  ipy: 'from IPython import embed; embed()'
  idc: 'from dataclasses import dataclass'
  ipd: 'import pandas as pd'
  inp: 'import numpy as np'
  ipl: 'import matplotlib.pyplot as plt'
  dc: |-
    @dataclass
    class $0:
  main: |-
    if __name__ == "__main__":
        $0
  inv: |-
    from invoke import task

    @task
    def $0
yaml:
  snip: |-
    $1: |-
      $0

rust:
  ddbg: '#[derive(Debug)]'
  dr: '#[derive($0)]'
  db: 'dbg!($0);'
  st: |-
    struct $1 {
      $0
    }
